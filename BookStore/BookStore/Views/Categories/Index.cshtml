@*@using System.Web.Mvc.WebViewPage<SelectList<BookStore.Models.Category>>*@
@model IEnumerable<BookStore.Models.Category>

@{
    ViewBag.Title = "Index";
}

@*@Html.DropDownList("NameCategory", ViewBag.NameCategory as SelectList)*@

@*@Html.DropDownList("CategoryId", "NameCategory")*@

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameCategory)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameCategory)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryId })
        </td>
    </tr>
}

</table>
